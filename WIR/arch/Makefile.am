SUBDIRS			= generic

if BUILD_ARM
  SUBDIRS	       += arm
endif

if BUILD_LEON
  SUBDIRS	       += leon
endif

if BUILD_RISCV
  SUBDIRS	       += riscv
endif

if BUILD_TRICORE
  SUBDIRS	       += tricore
endif
